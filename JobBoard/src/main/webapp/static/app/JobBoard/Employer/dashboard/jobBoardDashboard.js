'use strict';

define([
        'app',
        './config/dashboard',
        './controllers/dashboard',
        './services/dashboard'
], function(app) {
	console.log('dashboard modules loaded..');
});